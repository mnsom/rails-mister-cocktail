<div class="container mt-3">
  <h1>Our Cocktails🍸 ~Menu~</h1>
  <% @cocktails.each do |cocktail| %>
    <p><%= link_to cocktail.name, cocktail_path(cocktail) %></p>
  <% end %>
  <div class="mt-3">
    <%= link_to 'Add new cocktail', new_cocktail_path, class: "btn btn-success" %>
  </div>
</div>
